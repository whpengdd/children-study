// src/screens/Study/ProgressDots.tsx
//
// Re-export of the shared ProgressDots so callers in the Study folder can
// import from a local path. The real component lives in src/components/.

export { default } from "../../components/ProgressDots";
export type { ProgressDotsProps } from "../../components/ProgressDots";
